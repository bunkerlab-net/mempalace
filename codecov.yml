coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0.5%
    patch:
      default:
        target: 90%

comment:
  layout: "header, diff, files"
  require_changes: true
