name: Lint

on:
  workflow_call:

permissions: {}

jobs:
  run:
    name: ${{ matrix.toolchain }}
    runs-on: ubuntu-24.04
    continue-on-error: ${{ matrix.toolchain != 'stable' }}

    permissions:
      contents: read

    strategy:
      fail-fast: false
      matrix:
        toolchain: [stable, beta, nightly]

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@2c7215f132e9ebf062739d9130488b56d53c060c # master # zizmor: ignore[ref-version-mismatch]
        with:
          toolchain: ${{ matrix.toolchain }}
          components: clippy, rustfmt

      - name: Capture rustc fingerprint
        id: rustc
        run: echo "fingerprint=$(rustc -Vv | shasum -a 256 | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"

      - name: Setup Mise
        uses: jdx/mise-action@9e7f7633ff6f6d6048a9418a68d48f288f50eb14 # v4.2.3
        env:
          MISE_JOBS: 4
        with:
          cache_key_prefix: mise
          cache: true
          experimental: true
          install: true

      - name: Cache Cargo
        if: matrix.toolchain == 'stable'
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-lint-${{ steps.rustc.outputs.fingerprint }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-lint-${{ steps.rustc.outputs.fingerprint }}

      - run: cargo clippy --all-targets --all-features
      - run: cargo fmt --check --all
      - run: hk check --all --skip-step no_commit_protected_branch
        env:
          GITHUB_TOKEN: ${{ github.token }}
